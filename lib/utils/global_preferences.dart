import '../model/preferences.dart';

const CURRENCY = Preference('currency', String, 'PLN');
const DATE_FORMAT = Preference('dateFormat', String, 'yyyy-MM-dd');
const TIME_FORMAT = Preference('timeFormat', String, 'HH:mm');